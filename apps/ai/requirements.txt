# Runtime dependencies (mirror pyproject [project.dependencies]).
# Pin ranges kept in sync with pyproject; use `pip install -e .[dev]` for dev extras.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
redis>=5.0
